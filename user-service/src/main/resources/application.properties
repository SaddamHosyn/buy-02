server.port=8081
spring.application.name=user-service

# MongoDB Atlas Configuration
# Replace YOUR_PASSWORD with your actual password for saddamdb user
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://saddamdb:Saadi8189075@buy02-cluster.s0i27el.mongodb.net/userdb?retryWrites=true&w=majority}
spring.data.mongodb.database=userdb

# --- JWT Configuration ---
jwt.secret.key=dGhpc2lzYXNlY3VyZXNlY3JldGtleWZvcnRoZWJ1eWFwcGxpY2F0aW9udGhhdGlzbG9uZ2Vub3VnaGZvckhTMjU2

# --- Eureka Client Configuration ---
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
# Default: localhost:8761 (for you)
# Override: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE (for Docker)
eureka.client.service-url.defaultZone=${EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE:http://localhost:8761/eureka/}
# Default: localhost (for you)
# Override: EUREKA_INSTANCE_HOSTNAME (for Docker)
eureka.instance.hostname=${EUREKA_INSTANCE_HOSTNAME:localhost}

# Disable Spring Cloud compatibility check
spring.cloud.compatibility-verifier.enabled=false

# Kafka Configuration for Docker
spring.kafka.bootstrap-servers=${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer
